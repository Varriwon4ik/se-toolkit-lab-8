# Adapted from backend/Dockerfile
# Also see https://docs.astral.sh/uv/guides/integration/docker/

# ---
# Stage 1: build the application in the `/app` directory.
# ---

ARG REGISTRY_PREFIX_DOCKER_HUB
FROM ${REGISTRY_PREFIX_DOCKER_HUB}astral/uv:python3.14-bookworm-slim AS builder
ENV UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy

# Omit development dependencies
ENV UV_NO_DEV=1

# Disable Python downloads - use the system interpreter across both images.
ENV UV_PYTHON_DOWNLOADS=0

WORKDIR /app

# Copy workspace root pyproject.toml and uv.lock for workspace dependency resolution
COPY --from=workspace pyproject.toml uv.lock /app/

# Copy mcp directory for editable workspace dependency
COPY --from=workspace mcp /app/mcp

# Copy nanobot-websocket-channel for editable dependencies
COPY --from=workspace nanobot-websocket-channel /app/nanobot-websocket-channel

# Copy nanobot project files
COPY pyproject.toml ./

# Sync nanobot dependencies and install webchat channel
RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --frozen && \
    uv pip install --python /app/.venv/bin/python -e /app/nanobot-websocket-channel/nanobot-channel-protocol \
                              -e /app/nanobot-websocket-channel/nanobot-webchat

COPY . .

# ---
# Stage 2: use a final image without uv
# ---

ARG REGISTRY_PREFIX_DOCKER_HUB
FROM ${REGISTRY_PREFIX_DOCKER_HUB}python:3.14.2-slim-bookworm
# It is important to use the image that matches the builder, as the path to the
# Python executable must be the same.

# Set environment variables for Python and application
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
# Make sure we use UTF-8 encoding
ENV LANG=C.UTF-8

RUN apt-get update && apt-get install -y --no-install-recommends gosu && rm -rf /var/lib/apt/lists/*

# Setup a non-root user
RUN groupadd --system --gid 999 nonroot \
    && useradd --system --gid 999 --uid 999 --create-home nonroot

# Copy the application from the builder
COPY --from=builder --chown=nonroot:nonroot /app /app

# Place executables in the environment at the front of the path
ENV PATH="/app/.venv/bin:$PATH"
ENV PYTHONPATH="/app"

WORKDIR /app

# Run the nanobot gateway via entrypoint
CMD ["python", "/app/entrypoint.py"]
